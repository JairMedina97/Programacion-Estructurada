#include <stdio.h>

// intefer = numero entero
int main() { // main routine (where execution starts)
    int a, b, c, d, e; //variable
    a = 123; // asignment
    b = 15;
    c = a % b;
    d = a / b;
    e = d * b +c;
    printf("%i is the same as %i", e, a); // imprimir
    return 0;
}
